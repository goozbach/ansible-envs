Ansible Environment Example
###########################

I needed to prove out that we can change things with environment settings using
ansible.

To execute:

* Run `vagrant up`

* Run `vagrant ssh`

* Run `/vagrant/run_all.sh`
